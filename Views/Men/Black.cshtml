@using osu.golf.handicap.Models

@{
    ViewBag.Title = "Black";
}

@using (Html.BeginForm("Black", "Men")){
    <div>
        <h2>@Html.Label("Enter first score") : @Html.TextBox("score1")</h2>
        <h2>@Html.Label("Enter second score") : @Html.TextBox("score2")</h2>
        <h2>@Html.Label("Enter third score") : @Html.TextBox("score2")</h2>
        <h2>@Html.Label("Enter fourth score") : @Html.TextBox("score2")</h2>
        <h2>@Html.Label("Enter fifth score") : @Html.TextBox("score2")</h2>
    </div>
        <input type="submit" name="Go" value="Go"/>
}